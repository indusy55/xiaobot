services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: file:/app/data/local.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
